# Java 15 JavaFX Project

Example project setup for JFX 15 app that can compile with
os specific libraries depending on host to produce a fat jar.

Run using the gradle `run` task, and build the fat jar using the
gradle `jar` task.